type Usuario {
  id: Int
  nome: String
  idade: Int
  email: String
  perfil: Perfil
}
type Query {
  usuario(id: Int): Usuario
  usuarios: [Usuario]
}

type Mutation {
  criarUsuario(nome: String, idade: Int, email: String): Usuario!
}
